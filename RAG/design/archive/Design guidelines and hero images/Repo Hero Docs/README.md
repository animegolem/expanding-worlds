# Repo hero docs — 2026-07-07

The three canonical design documents that live in the repo (RAG/design/).
Keep the folder intact: the documents load `support.js` / `doc-page.js`
by relative path. Open any `.dc.html` directly in a browser.

1. **The Two Materials** — the North Star. Read before your first UI
   pull request: UX-layer features are mono; canvas-level features are
   physics. The lifecycle and beat animations show the feel; the
   Desk-or-Terminal Rulebook (in Design-Artifacts-v2.0) is the
   enforceable checklist version.
2. **UI Vision v2** — the hero images: seven canonical app states.
3. **Usage Storyboard** — how it works, function by function (20
   sections, replaces How It Works v1).

Decision trail for everything depicted: Design-Team-Letter-1.md in
Design-Artifacts-v2.0.
</content>
