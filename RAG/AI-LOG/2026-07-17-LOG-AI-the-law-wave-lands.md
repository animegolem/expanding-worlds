---
node_id: 2026-07-17-LOG-AI-the-law-wave-lands
tags:
  - AI-log
  - development-summary
  - design-adoption
  - release
closed_tickets:
  - AI-IMP-301
  - AI-IMP-302
  - AI-IMP-303
  - AI-IMP-304
  - AI-IMP-305
created_date: 2026-07-17
related_files:
  - RAG/AI-EPIC/AI-EPIC-030-the-ratified-law-wave.md
  - RAG/RFC-0001-Core-Note-Node-and-Canvas-Model.md
  - RAG/design/promises/dock.md
  - RAG/design/DESIGN-GAPS.md
confidence_score: 0.92
---

# 2026-07-17-LOG-AI-the-law-wave-lands

## Work Completed

The feel pass became law became enforcement, in one arc: kits 1.5
(takeover chrome ruling 40) and 1.6 (the owner's same-day answer to
the code-lead letter — min width 960, click-swallow law, density
TRIAD, floating-window law, THE PROPORTION LAW drawn, geometry
contract into the preflight) were ratified as RFC rev 0.73; the
promise ledger reached rev 4 with ZERO open dependencies (fully
cosignable); EPIC-030 was cut (AI-IMP-301..305) and ran as one
Codex wave to close. v0.26.0 tagged; main at 9d356218.

- **Rev 0.73** (`0b5f8b71`): §8.2 rail = board-mode resident +
  takeover band identity (lead ruled the strip's universal ☰ never
  duplicates); §8.8.3 strip-46 correction, 960 floor, triad (only
  TOUCH changes the frame); §8.8.5 proportion law incl.
  floating-window law; §8.8.6 dismissal swallow; §8.5 posture verbs
  (fly-to-read / pin-to-world / tape-to-glass, equal-height bind
  with measure-driven width). Queue pruned into the RFC; touch band
  NUMERICS still owed by the kit (DESIGN-GAPS, `f98035c3`).
- **The wave, four rounds**: r1 pre-implementation review corrected
  the tickets (headline: shipped "comfortable" was the old TOUCH
  mapping mislabeled — strip 0/44px — verified in source before
  ruling); r2 implemented with the evidence-bundle pilot catching
  two defects pre-submission (restore-toast swallowed over Trash;
  picker containment) and CONVICTING the dock displacement
  (column-reverse, exactly the diagnosis-first design of 305); r3
  fixed shard-1 (GEO-03 sub-pixel tolerance; a real Search
  mount-order hypothesis that turned out wrong); r4 retracted r3's
  diagnosis in writing and convicted the true cause — OutlineView's
  earlier capture listener eating Escape whenever Search autofocus
  hadn't settled (macOS won the focus race habitually; Windows/
  Linux deterministically lost it) — fixed at the TakeoverLayer
  host with the unfocused case pinned. Oracle run 29544465545
  "success"; floors 659/410/578/279 (CI-mode; 3 perf tests are the
  local hardware gate — the 282-vs-279 reconciliation).
- **The trains earned their keep**: wave-gate halted five times,
  every halt correct (dirty tree; count divergence that reconciled
  to the perf gate; red oracle twice; a GitHub 503 window); the
  close train refused once, correctly, until close records existed.
- **Owner-side**: kit packages archived with 1.6 kit-normative;
  KIT-CHANGELOG 1.5+1.6 entries; HUMAN-TESTING gains five v0.26.0
  checks; CHANGELOG two-voiced; the repo-bootstrap PUBLIC skill
  (~/git/_tooling_) gained the consultation lane + two-ear notice
  harness, genericized (`4416d61`).

## Issues Encountered

- **The lead's own ear failed the harness's law**: round 2 landed
  in a watcher re-arm gap and was BASELINED AWAY (the restart sin
  PH-001's rebuild forbids, committed by my script). Fixed: v3
  watcher diffs against a PERSISTED acked fingerprint (`--ack` on
  process); re-arms can no longer eat arrivals.
- The cwd-relative bite recurred (6th); a pathspec-filtered git
  diff returned empty against a name-status M (unexplained local
  oddity — verified at blob level instead; note if it recurs).
- Round-3's "only SourcePanel fails" prediction did not survive
  the runners — platform convictions come from artifacts, not
  local passes; the verdict said so and round 4 delivered.

## Tests Added

Via the merge: desktop unit 570→578, e2e 274→279 (CI-mode) + 3
perf-gate tests — including the GEO-03/LAYER-01 permanent law
pins, the dismissal-swallow census suite, takeover
migration/reservation assertions, the three-door MRU census, and
the layered-Escape unfocused-case pin.

## Next Steps

- **Owner, one at a time**: (1) the dock-ledger COSIGN (rev 4,
  zero dependencies; two missing-promise findings + one correction
  request from the wave await it); (2) the `launchctl bootout` go
  for the approved v2 outbox watcher; (3) touch band numerics at
  the next kit sitting.
- **The notes epic** is the next big cut: charter = §8.5 posture
  verbs + §8.8.5 proportion/floating-window law + the zoom-verb
  residuals (queue-annotated) + big-editor role.
- Standing: v0.26.0 release body edit when the workflow finishes
  (For-testers voice above the xattr note); AI-IMP-269 #2
  (SourcePanel race — failed once again this wave) and #3 (CI
  1s/step) remain cuttable; kit pages still show a bar-☰ the RFC
  overrules (small redraw, queued in KIT-CHANGELOG note).
