---
node_id: 2026-07-24-LOG-AI-the-handshake-and-the-receiver
tags:
  - AI-log
  - development-summary
  - infrastructure
  - process
  - release
closed_tickets:
  - AI-IMP-307
  - AI-IMP-308
  - AI-IMP-309
  - AI-IMP-310
  - AI-IMP-311
created_date: 2026-07-24
related_files:
  - RAG/OWNER-APPROVALS.md
  - .codex/PROTOCOL.md
  - .codex/WORK-LEDGER.md
  - RAG/AI-EPIC/AI-EPIC-031-the-notes-epic.md
  - RAG/scripts/review-metrics.sh
confidence_score: 0.92
---

# 2026-07-24-LOG-AI-the-handshake-and-the-receiver

## Work Completed

The window where the shop learned the difference between
instrumenting a problem and removing it.

- **v0.26.2 SHIPPED (the alph wave, 307–311):** all five field
  defects convicted at the correct layer, gated (counts
  667/423/590/288 over floors; oracle "success" 29629902495),
  merged `317ef796`, tagged with all three installers attached —
  after repairing my own release-order collision (workflow CREATES
  the release; the lead edits the body after; saved to memory).
  Mid-wave, Codex's STOP caught my accepted 310 ruling citing a
  nonexistent payload seam; the world-unit `diameter` ruling
  replaced it.
- **EPIC-031 chartered into work:** the round-0 census corrected
  the epic's premise (the loose-note panel EXISTS), convicted four
  cross-posture law violations, and cut BY LAW → AI-IMP-312..315
  + note-paper promise ledger rev 2 (13 cards). Wave round 2
  implemented all four (1809 insertions); local gate fully green;
  the ORACLE convicted two Linux-only failures (sticky invariance
  at the texture rung; a rename dirty-indicator regression) →
  round 3 amend PARKED pending the executor picking it back up.
- **The Phase-C receiver: designed, built, lab-proven, merged**
  (tooling `b8ab13e`): claim/renew/release/fail carrier verbs with
  a named 5-event claim bound, tokenized leases, receipt-first
  attempt facts, atomic pre-injection `confirm` (replays labeled
  HISTORY — the live trust-wave replay specimen made structural),
  history-first crash recovery, worker-never-ACKs. Lead
  independently reran 24/24 unit + 15/15 fault-matrix scenarios +
  byte-identity ×3. STATE 2.5: lab-proven, NOT live — canary at
  Codex's discretion, then ONE reserved owner-go (v1 watcher
  teardown + shadow install; rollback = pause worker, resume
  heartbeat).
- **The process arc — three failures, one shape:** ACK-mistaken-
  for-disposition (work ledger born), filler-equals-never (lane
  promotion), executor deadlock (prerequisite rows with provision
  owners). The signed EXECUTION HANDSHAKE (state machine, evidence
  bars, mortal-sitting rule, cadence-derived degradation
  thresholds) made stalls visible — then the owner's correction
  caught us building an owner-mediated control plane out of it.
  STANDING-AUTHORITY-NO-BOUNCE ratified: approval is durable
  capability; escalation is semantic, not procedural.
- **RAG/OWNER-APPROVALS.md (owner-designed):** durable approval
  ledger — live entries never re-asked, reserved entries make the
  un-approved explicit, escalations are logged deltas. The lead is
  registrar and chaser. Backfilled A1–A8 + A-RES1..4.
- **Review-metrics trial (A8):** gate-time targeting report
  (churn×complexity hotspots, platform-risk API touches, CCN
  deltas) appended to wave evidence; CC-as-gate rejected. Dry-run
  flagged exactly the two files behind the live oracle convictions.
- **Liveness ladder** ratified into PROTOCOL.md after Codex's
  premature "Phase-C live" reading deleted its own heartbeat and
  stranded three events ~11h. Ears are removed only at state 3 per
  direction.

## Issues Encountered

- The lead's ledger claims outran the file twice (a verdict said a
  schema column had landed before it had; a "started" outlived its
  rebooted task) — both caught by Codex against the artifacts, both
  now structurally checkable (validator + progress_at).
- The delivery-degraded monitor's FIRST patrol caught the lead's
  own missing ack receipt (516m vs the 120m threshold) — my
  post-reboot probe rebuild had silently dropped the auto-ack step.
- Two machine reboots wiped /private/tmp clones mid-wave; the
  fetched-branch-in-canonical pattern made both losses zero-cost.
- The harness began reaping long-running background watchers;
  replaced with bounded 60m probes whose every completion (or kill)
  re-arms the ear.

## Tests Added

Notes wave round 2 (parked at amend): +5 canvas-engine (paper
ruler), +6 desktop, +8 e2e pending its round 3. Tooling: 24 stdlib
unit tests + 15-scenario scripted fault matrix. No product floors
moved (notes wave unmerged).

## Next Steps

- Notes wave round 3: executor re-clones from canonical
  `codex/notes-wave@fbcb4ef7`, fixes the two Linux convictions →
  full gate (now emits the A8 metrics report) → EPIC-031 wave 1
  closes → 320/321 brief to Codex.
- Phase-C: disposable persistent canary (Codex discretion) → the
  single A-RES2 owner decision with packaged evidence.
- OWNER (one at a time): dock cosign (A-RES3) · shortcuts +
  board-orphan gos (A-RES4) · design residuals sitting (A-RES1).
- The ArXiv paper (OCD / predictive processing / toy model) —
  standing offer of lit-anchoring, model-building, or hostile
  review whenever the owner wants a collaborator.
