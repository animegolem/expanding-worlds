> PLACEHOLDER — this file should hold a real `verdict: amend` from
> your instance. The shipped EXAMPLE-VERDICT.md is an accepted —
> the victory lap — but the amend path (numbered amendments,
> same-branch fix, round bump, resubmit-by-rewrite) is the one a
> new orchestrator is actually unsure how to write. A fabricated
> example would betray the verbatim promise the other references
> make, so this stays empty until a genuine one is harvested from
> an outbox. Until then, `templates/VERDICT-TEMPLATE.md` defines
> the shape.
